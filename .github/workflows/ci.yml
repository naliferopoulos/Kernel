name: Kernel CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: Build Toolchain
      run: |
        cd kernel
        rm -rf toolchain/*
        make toolchain
    - name: Build Kernel
      run: |
        cd kernel
        make
